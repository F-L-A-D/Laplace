apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: laplace-web
spec:
  template:
    spec:
      containers:
        - image: gcr.io/gen-lang-client-0262945179/laplace-web
          ports:
            - containerPort: 3000
          env:
            - name: NODE_ENV
              value: production